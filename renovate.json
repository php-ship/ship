{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": ["config:recommended"],
    "customManagers": [
        {
            "customType": "regex",
            "description": "Docker image tags pinned as PHP string literals in ServiceDefinition classes -- Renovate's built-in dockerfile manager only scans actual Dockerfiles (which it already covers natively: stubs/docker/php/Dockerfile*, stubs/docker/garage/Dockerfile), not these.",
            "fileMatch": ["^src/Services/.*\\.php$"],
            "matchStrings": [
                "'image' => '(?<depName>[^:']+):(?<currentValue>[^']+)'"
            ],
            "datasourceTemplate": "docker"
        },
        {
            "customType": "regex",
            "description": "Mutagen's pinned release version, used by the docker-build CI job to test the opt-in SHIP_MUTAGEN sync mode (Ship\\Sync\\MutagenSync) against a real Docker daemon.",
            "fileMatch": ["^\\.github/workflows/ci\\.yml$"],
            "matchStrings": [
                "MUTAGEN_VERSION=(?<currentValue>v\\d+\\.\\d+\\.\\d+)"
            ],
            "depNameTemplate": "mutagen-io/mutagen",
            "datasourceTemplate": "github-releases"
        }
    ],
    "packageRules": [
        {
            "description": "Every pinned image here is checked and chosen deliberately (see docs/adding-a-service.md's \"Pin image tags\" note) -- group them into one PR per run instead of one PR per image, so a version bump gets one human review pass, not several.",
            "matchManagers": ["custom.regex", "dockerfile"],
            "groupName": "Docker image tags"
        }
    ]
}
